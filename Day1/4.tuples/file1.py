# tuple data structure
# tuple can store any data type
# tuples are immutable

example = ('one', 'two', 'three')
# no append, no insert, no pop, no remove
# tuples are faster
# we can use methods --> count, index, len function, slicing
print(example[0:2])

